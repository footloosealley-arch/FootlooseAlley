"use client";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { CLASS_DAYS } from "@/services/classes.service";
interface Props { search: string; day: string; program: string; status: string; programs: string[]; onSearch: (v:string)=>void; onDay:(v:string)=>void; onProgram:(v:string)=>void; onStatus:(v:string)=>void }
export default function ClassFilters(p: Props) { return <div className="grid gap-3 border-b p-4 md:grid-cols-4"><Input type="search" aria-label="Search classes" placeholder="Search classes or instructors" value={p.search} onChange={e => p.onSearch(e.target.value)} /><Select value={p.day} onValueChange={v => p.onDay(v ?? "All")}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All">All days</SelectItem>{CLASS_DAYS.map(x => <SelectItem value={x} key={x}>{x}</SelectItem>)}</SelectContent></Select><Select value={p.program} onValueChange={v => p.onProgram(v ?? "All")}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All">All programs</SelectItem>{p.programs.map(x => <SelectItem value={x} key={x}>{x}</SelectItem>)}</SelectContent></Select><Select value={p.status} onValueChange={v => p.onStatus(v ?? "All")}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="All">All statuses</SelectItem><SelectItem value="Active">Active</SelectItem><SelectItem value="Inactive">Inactive</SelectItem></SelectContent></Select></div> }
